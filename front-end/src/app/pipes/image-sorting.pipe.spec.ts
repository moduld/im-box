import { ImageSortingPipe } from './image-sorting.pipe';

describe('ImageSortingPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
