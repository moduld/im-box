import { EndNgForDirective } from './end-ng-for.directive';

describe('EndNgForDirective', () => {
  it('should create an instance', () => {
    const directive = new EndNgForDirective();
    expect(directive).toBeTruthy();
  });
});
