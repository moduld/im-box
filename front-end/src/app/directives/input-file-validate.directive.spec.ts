import { InputFileValidateDirective } from './input-file-validate.directive';

describe('InputFileValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFileValidateDirective();
    expect(directive).toBeTruthy();
  });
});
